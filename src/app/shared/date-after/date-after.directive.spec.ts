import { DateAfterDirective } from './date-after.directive';

describe('DateAfterDirective', () => {
  it('should create an instance', () => {
    const directive = new DateAfterDirective();
    expect(directive).toBeTruthy();
  });
});
